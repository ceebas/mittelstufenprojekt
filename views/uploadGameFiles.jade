include ./layout/head.jade
body
	.container
		include ./layout/nav.jade
		.panel.panel-default
			.panel-heading 
				h3#createCaption Lade Datein für dein Spiel hoch
			.panel-body
				form(action='', methode='POST')
					script.
						var gameObject = !{JSON.stringify(gameObj)}
					.vorschaubild
						label(for='preview') Vorschaubild
						input#vorschau(type='file', name='preview')
					.spielerform
						label(for='player') Spieler Form
						input(type='file', name='player')
					.schussform
						label(for='shoot') Schuss Form
						input(type='file', name='shoot')
					.gegnerform
						label(for='foes') Gegner Form
						input(type='file', name='foes')
					input#senden(type='submit', name='senden', value='hochladen')
	include ./layout/footer.jade